T-0086 — the forged-identity probe, and its mutation proof
==========================================================

The instrument: an HONEST payload, built by `arreo sync payload` on the laptop, with
its `machine` field flipped to alpha's device id — a line-anchored rewrite of the JSON
the CLI printed (`rewrite_field`, which walks the string to the field's closing quote,
so the payload's embedded document cannot confuse it). The digest covers `content`,
which is untouched, so the payload passes every other check and the identity check in
`serve_sync` is the only thing left that can refuse it.

WITH the check:
  [PASS] mesh: a payload claiming another machine's identity is refused, not corrected
  mesh: forged-identity probe — dev_540ef98d6a9ad8da8c80ad651d627f83 claimed dev_d008f2e7fd0a653bcc38d1a0b91236b4

WITHOUT the check (mutation: the `if payload.machine != device { return refused(..) }`
block deleted from crates/arreo-server/src/daemon.rs::serve_sync, rebuilt, re-run):
  [FAIL] mesh: a payload claiming another machine's identity is refused, not corrected
  [FAIL] mesh: none of the three refusals wrote anything on the receiver
  the receiver said: (empty — the forged payload was ACCEPTED)
  mesh: 32 passed, 1 skipped, 4 failed

The attack, observed: the forged `machine` claim is absorbed as the sender's own
counter key, which is F2's class reopened by the network — exactly what the check
exists to stop. The check was restored afterwards and the file is byte-identical to
the reviewed version (`grep -c MUTATION crates/arreo-server/src/daemon.rs` = 0).

Full mutation transcript: target/test-scratch/T-0086/mutation3.txt
