T-0048 secret scan — full history, pinned gitleaks 8.28.0
==========================================================

Scanner: gitleaks 8.28.0 (linux x86_64)
URL: https://github.com/gitleaks/gitleaks/releases/download/v8.28.0/gitleaks_8.28.0_linux_x64.tar.gz
Published SHA256: a65b5253807a68ac0cafa4414031fd740aeb55f54fb7e55f386acb52e6a840eb
Downloaded SHA256: a65b5253807a68ac0cafa4414031fd740aeb55f54fb7e55f386acb52e6a840eb (match — verified before execution)

Command:
  gitleaks detect --source . --log-opts="--all" --redact --no-banner \
    --report-format json --report-path /tmp/gitleaks-full.json .

Coverage: 106 commits scanned (git log: 106 commits), 3.78 MB, fixtures/ and
.loop/evidence/ included, nothing excluded. Raw report: secrets-scan.json
(redacted form — gitleaks --redact replaces secret values with REDACTED).

Findings: 8, ALL FALSE POSITIVES, each disproved from the code that produced it:

1. generic-api-key | .loop/evidence/T-0045/transcript.txt:33 | 357d35c7
   `daemon_key=9886beb2…` — an Ed25519 PUBLIC dial key, printed by a T-0045
   transcript showing the machine table's columns. Public keys are published by
   design (the relay writes them from the authenticated certificate; clients need
   them to dial). Not a secret.

2-4,7,8. generic-api-key / curl-auth-header | crates/arreo-core/tests/audit.rs:118,157
   crates/arreo-server/tests/audit.rs:388 | crates/arreo-core/tests/store.rs:41
   crates/arreo-core/tests/fixtures.rs:37 | 07ffa1d6, 15e42e85, 55409d6d
   `sk-abc123XYZ4567890abcdef`, `ghp_AAAABBBBCCCCDDDDEEEEFFFF` — dummy literals
   that exist ONLY as inputs to the redaction tests, each paired with an assertion
   that the value does NOT survive (audit.rs, fixtures.rs `secret_scan_flags_keys…`).
   A scanner flagging the test for the scanner is the expected shape, not a leak.

5-6. generic-api-key | .loop/evidence/T-0024/happy-path.txt:4,5 | f2750560
   `server_key` — the PUBLIC pairing root key, embedded in the `arreo://pair` URI
   the transcript records (meant to be handed to the phone/QR). Public by design.

Verdict: NO SECRET HAS EVER BEEN COMMITTED. No rotation, no history rewrite needed.
The criterion is met in substance; met literally once a .gitleaks.toml allowlist
covers the four dummy-token/test shapes above (recommended config in the SecretsScan
worker deliverable — not applied here, because an allowlist is a policy change that
belongs in its own review, and the raw findings above are the honest record).

---

## Addendum 2026-09-13 — five more false positives, triaged the same way

Scanner: the same pinned gitleaks 8.28.0, run exactly as CI runs it
(`gitleaks detect --source . --log-opts="--all" --redact --no-banner`).

**Measured state before this addendum: the gate was RED.** One finding survived the
allowlist, from a transcript revision that no longer exists in HEAD but does in history:

```
$ gitleaks detect --source . --log-opts="--all" --redact
1:13PM WRN leaks found: 1
$ echo $?
1
```

5. `generic-api-key` | `.loop/evidence/T-0067/transcript.txt:13` | commit `ec4034a`
   `relay.db account.root_key = b7982e04…` — an Ed25519 **public** account root key,
   printed by the first-machine bootstrap test to show what the relay was told. The
   secret half never leaves the machine that made it; the public half is what
   `account add --root-key` is *for* (docs/machines.md: "the public key (64 hex
   characters) — this is what `account add --root-key` takes"). The line was corrected
   in the following commit (`199d1a9`), so it exists only in history — which is exactly
   what `--log-opts="--all"` scans. Allowlisted by exact value.

The remaining four are the dummy values T-0075's secret-scanner probe **writes and then
executes** (`.loop/evidence/T-0075/existing-scan-secrets-e2e.txt`): sequential filler in
four providers' key shapes plus the jwt.io example token, used as *input* to
`arreo record` to prove what the shipped scanner does and does not catch. They are the
witnesses of the finding that `"apiKey": "{env:VAR}"` is refused while
`token: vbk_pro_…` is missed — rewriting them to look less key-shaped would make the
transcript unreproducible. Allowlisted by exact value:

6. `gcp-api-key` | `AIzaSyA0123456789abcdefghijklmnopqrstuv` (sequential filler)
7. `generic-api-key` | `xai-0123456789abcdefghijklmnopqrstuvwx` (sequential filler)
8. `generic-api-key` | `sk-0123456789abcdefghijklmnop` (sequential filler; the existing
   `sk-` entry is a different, shorter literal)
9. `jwt` | `eyJhbGciOiJIUzI1NiJ9.eyJzdWIiOiIxMjM0NTY3ODkwIn0.dozjgNryP4J3jVmNHl0w5N_XgL0n3I9PlFUP0THsR8U`
   — the jwt.io documentation example token, verbatim, chosen because it is the
   canonical shape an implementer types when testing a JWT rule.

**After:** both scans clean — history (`--log-opts="--all"`, 166 commits, 6.2 MB) `no
leaks found`, exit 0, and the working tree (`--no-git`, 2.83 GB including `target/`) `no
leaks found`, exit 0.
