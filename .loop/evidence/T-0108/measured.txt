T-0108 — the two facts that decide this task, measured (not read)
git 2.47.3, Linux 6.12.101+deb13-cloud-amd64

=== FACT 1: does `git worktree remove` (no --force) refuse a DIRTY checkout itself? ===
(this is the question the finding turns on: if git re-checks, the file-loss window the
 hypothesis describes is closed by git, and our status() read is only the message)

$ git worktree add -b arreo/wt /home/dev/dev/Arreo/target/test-scratch/T-0108/probe2/wt HEAD   # then: echo dirty > /home/dev/dev/Arreo/target/test-scratch/T-0108/probe2/wt/untracked.txt

$ git worktree remove /home/dev/dev/Arreo/target/test-scratch/T-0108/probe2/wt
fatal: '/home/dev/dev/Arreo/target/test-scratch/T-0108/probe2/wt' contains modified or untracked files, use --force to delete it
exit=128

still present afterwards:
  f.txt
  untracked.txt

=== FACT 2: what does the daemon's kill send to a child that ignores SIGTERM? ===
(wait_timeout returning None is the branch the fix adds; if SIGKILL is sent, the
 leader always dies, so that branch is reachable only by a process the signal cannot
 finish off — uninterruptible sleep — or by a signal that was never delivered)

--- and the probe run itself (throwaway test, deleted after this transcript) ---
$ cargo test -p arreo-server --test zz_probe_kill -- --nocapture
PROBE child_pid              = Some(1528569)
PROBE wait(200ms) before kill = None
PROBE kill_shared()           = Ok(())
PROBE wait(5s) after kill     = Some(Exited(1))
PROBE /proc/1528569 exists       = false
PROBE kill_shared() again     = Err(Io(Os { code: 3, kind: Uncategorized, message: "No such process" }))
test result: ok. 1 passed; 0 failed; 0 ignored; 0 measured; 0 filtered out; finished in 1.04s
