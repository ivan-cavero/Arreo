

- 2026-09-11 [turn 32] T-0032 remote TUI attach completed and T-0054 relay peer-departure landed (split out of T-0032 within the same turn, then done): the client half of T-0032 was already proven (target seam, long-lived connection, backoff reconnect, cursor-owned resume, viewer refusal, `relay` slice 14/14, ADR 0015); T-0054 added `RelayKind::PeerGone` (relay announces departures account-wide, a device cannot originate one, `read_pump` ends the affected stream with a typed reason) **and** the deeper fix that a dropped `RelaySession` aborts its pump tasks so the connection actually closes — without which the relay never learned a client had left. Measured: a reconnect after a killed client went from >60 s to 77 ms with a byte-identical transcript. ADR 0016; tests in `arreo-tui/tests/remote.rs`, `arreo-server/tests/relay_client.rs`, `arreo-core/src/relay/mod.rs`; evidence `.loop/evidence/T-0054/`. T-0032's last criterion (§3.14 reattach) split to T-0055 (needs a relay clock seam) and T-0036's human gate recorded in its task file. 332 workspace tests, clippy/fmt clean, vet/deny/audit green, check-targets PASS/SKIP, 6 e2e slices green, bench 6/6; remote == local
