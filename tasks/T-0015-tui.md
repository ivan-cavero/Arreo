---
id: T-0015
title: ratatui TUI — panes, sidebar with state + RAM, mouse-first
phase: 1
priority: 4
status: todo
depends_on: [T-0005, T-0006, T-0013]
scope:
  - crates/arreo-tui/**
  - .loop/evidence/T-0015/**
---

## Goal

The TUI clients deserve: sidebar of agents (state dots + RAM bars), pane wall, mouse-first
split borders (Herdr's proven UX), themed with the engine from T-0016.

## Acceptance criteria

- [ ] Sidebar: panes grouped by state with dots + pulse for question; RAM per agent.
- [ ] Split/resize/focus with mouse; keyboard equivalent for everything.
- [ ] Rendering: grid deltas from the daemon, no full repaints on steady state.
- [ ] **Interactive evidence (PROMPT.md §6):** scripted PTY drives the TUI with real key
      events — resize, click, scrollback search — with frame captures in
      `.loop/evidence/T-0015/`; unit tests alone do not close this task.

## Verification

```console
cargo xtask e2e --slice tui
cargo xtask e2e --slice tui --interactive-evidence
```
